{
  "name": "firebase-todo",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "vite build && firebase deploy",
    "serve": "vite build && firebase serve",
    "emulate": "firebase emulator:start"
  },
  "dependencies": {
    "firebase": "^9.16.0",
    "vue": "^3.2.45"
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "^4.0.0",
    "vite": "^4.0.0"
  }
}
